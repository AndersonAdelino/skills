# Default mode (silence cut + normalization) — offline, no cost
auto-editor>=29.3,<30
ffmpeg-normalize>=1.42

# --fillers mode needs no extra package: transcription goes out over urllib
# from the stdlib, and the judgment happens in the agent, not in a remote LLM.

# Not on pip: ffmpeg and ffprobe must be on your PATH.
#   Windows: winget install Gyan.FFmpeg
#   macOS:   brew install ffmpeg
#   Linux:   sudo apt install ffmpeg
