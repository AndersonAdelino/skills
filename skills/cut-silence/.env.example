# Only needed for --fillers mode (filler-word and stutter removal).
# The default mode (silence cut + normalization) runs offline and needs no key.
#
# Copy this file to .env in the same folder and fill in the key:
#   cp .env.example .env
#
# Create a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=
